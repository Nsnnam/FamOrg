node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime data: family database, secrets, backups, uploads — never commit
data/
*.db
*.db-wal
*.db-shm
# Added by code-review-graph (local dev tooling — machine-specific paths)
.code-review-graph/
.mcp.json
.claude/
